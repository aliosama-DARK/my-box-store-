# قاعدة البيانات — بيانات العملاء وكلمات السر لا تُرفع أبداً
data/

# صور مرفوعة من لوحة التحكم (خاصة بكل بيئة تشغيل)
images/uploads/

# صور العملاء المرفقة بالطلبات (بيانات خاصة — لا تُرفع أبداً)
images/order_uploads/

# مشاريع أخرى في نفس المجلد (ليست جزءاً من المتجر)
GAME/
godot/
gravity/
shooter/
itch_assets/
PlayGodot.bat
PlayGravity.bat
PlayShooter.bat
ChatGPT Image*.png

# نظام التشغيل
Thumbs.db
desktop.ini
node_modules/

# أسرار البيئة — لا تُرفع أبداً
.env
.env.*
!.env.example

# مخرجات النشر والبناء
.vercel
.next
dist

# توكن Vercel المحلي (لا يُرفع أبداً)
.vercel-token

.vercel
.env*

# بيانات دخول الأدمن المحلية (لا تُرفع)
ADMIN_CREDENTIALS.txt
